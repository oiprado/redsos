 MATCH(s:Service { status: {status} }) RETURN
        UNION\n +
        MATCH(c:Person { guid: {user} })-[cr:CREATE]->(s:Service)\n +
        RETURN s \n +
        UNION\n +
        MATCH(c:Person { guid: {user} })-[cr:ATTEND]->(s:Service)\n +
        RETURN s
